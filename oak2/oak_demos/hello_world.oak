include!("/usr/include/oak/std/std_io.oak");
// package!("std");

let println<T>(what: T) -> void
{
    print(what);
    print("\n");
}

let main(argc: i32, argv: ^str) -> i32
{
    print("Hello, world!\n");

    println<i32>(65);

    0
}
