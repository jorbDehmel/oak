// For testing Oak's loops
// Legacy passed

package!("std");
use_rule!("std");

let main() -> i32
{
    let flag: i32;
    flag = 10;

    while (flag > 0)
    {
        print(flag);
        print("\n");

        flag -= 1;
    }

    0
}
