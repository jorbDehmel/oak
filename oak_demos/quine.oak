// A test of Quine functionality via __CONTENTS__
// Legacy passed

// Jordan Dehmel, 2023'

package!("std");

let main() -> i32
{
    // Quines (programs which print their source code)
    // are very easy lol
    print(__CONTENTS__);
}
