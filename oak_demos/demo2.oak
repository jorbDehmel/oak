//include!(oak/demo.oak);
link!(hello_world.o);

let test: struct
{
    data: u16,
    large_data: u32,
}

let sub: struct
{
    large_large_data: u64,
}

let test.add(self: *test) -> u32
{
    self.data + self.large_data
}

// A main function demo-ing Oak
let main(argc: i32, argv: *str) -> i32
{
    let b: i16 = 5;
    let c: u16 = 12;
    let d: i128 = 1234;

    0
}
