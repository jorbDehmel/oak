/**
 * @file
 * @brief Casts in Oak. These all interface to corresponding C
 * functions. This file is procedurally generated, so no
 * function-level documentation will be attempted.
 */

pragma!("no_dialect");
link!("std/casts.o");

let to_u8(what: i8) -> u8;
let to_i16(what: i8) -> i16;
let to_u16(what: i8) -> u16;
let to_i32(what: i8) -> i32;
let to_u32(what: i8) -> u32;
let to_i64(what: i8) -> i64;
let to_u64(what: i8) -> u64;
let to_f32(what: i8) -> f32;
let to_f64(what: i8) -> f64;
let to_bool(what: i8) -> bool;
let to_i8(what: u8) -> i8;
let to_i16(what: u8) -> i16;
let to_u16(what: u8) -> u16;
let to_i32(what: u8) -> i32;
let to_u32(what: u8) -> u32;
let to_i64(what: u8) -> i64;
let to_u64(what: u8) -> u64;
let to_f32(what: u8) -> f32;
let to_f64(what: u8) -> f64;
let to_bool(what: u8) -> bool;
let to_i8(what: i16) -> i8;
let to_u8(what: i16) -> u8;
let to_u16(what: i16) -> u16;
let to_i32(what: i16) -> i32;
let to_u32(what: i16) -> u32;
let to_i64(what: i16) -> i64;
let to_u64(what: i16) -> u64;
let to_f32(what: i16) -> f32;
let to_f64(what: i16) -> f64;
let to_bool(what: i16) -> bool;
let to_i8(what: u16) -> i8;
let to_u8(what: u16) -> u8;
let to_i16(what: u16) -> i16;
let to_i32(what: u16) -> i32;
let to_u32(what: u16) -> u32;
let to_i64(what: u16) -> i64;
let to_u64(what: u16) -> u64;
let to_f32(what: u16) -> f32;
let to_f64(what: u16) -> f64;
let to_bool(what: u16) -> bool;
let to_i8(what: i32) -> i8;
let to_u8(what: i32) -> u8;
let to_i16(what: i32) -> i16;
let to_u16(what: i32) -> u16;
let to_u32(what: i32) -> u32;
let to_i64(what: i32) -> i64;
let to_u64(what: i32) -> u64;
let to_f32(what: i32) -> f32;
let to_f64(what: i32) -> f64;
let to_bool(what: i32) -> bool;
let to_i8(what: u32) -> i8;
let to_u8(what: u32) -> u8;
let to_i16(what: u32) -> i16;
let to_u16(what: u32) -> u16;
let to_i32(what: u32) -> i32;
let to_i64(what: u32) -> i64;
let to_u64(what: u32) -> u64;
let to_f32(what: u32) -> f32;
let to_f64(what: u32) -> f64;
let to_bool(what: u32) -> bool;
let to_i8(what: i64) -> i8;
let to_u8(what: i64) -> u8;
let to_i16(what: i64) -> i16;
let to_u16(what: i64) -> u16;
let to_i32(what: i64) -> i32;
let to_u32(what: i64) -> u32;
let to_u64(what: i64) -> u64;
let to_f32(what: i64) -> f32;
let to_f64(what: i64) -> f64;
let to_bool(what: i64) -> bool;
let to_i8(what: u64) -> i8;
let to_u8(what: u64) -> u8;
let to_i16(what: u64) -> i16;
let to_u16(what: u64) -> u16;
let to_i32(what: u64) -> i32;
let to_u32(what: u64) -> u32;
let to_i64(what: u64) -> i64;
let to_f32(what: u64) -> f32;
let to_f64(what: u64) -> f64;
let to_bool(what: u64) -> bool;
let to_i8(what: f32) -> i8;
let to_u8(what: f32) -> u8;
let to_i16(what: f32) -> i16;
let to_u16(what: f32) -> u16;
let to_i32(what: f32) -> i32;
let to_u32(what: f32) -> u32;
let to_i64(what: f32) -> i64;
let to_u64(what: f32) -> u64;
let to_f64(what: f32) -> f64;
let to_bool(what: f32) -> bool;
let to_i8(what: f64) -> i8;
let to_u8(what: f64) -> u8;
let to_i16(what: f64) -> i16;
let to_u16(what: f64) -> u16;
let to_i32(what: f64) -> i32;
let to_u32(what: f64) -> u32;
let to_i64(what: f64) -> i64;
let to_u64(what: f64) -> u64;
let to_f32(what: f64) -> f32;
let to_bool(what: f64) -> bool;
let to_i8(what: bool) -> i8;
let to_u8(what: bool) -> u8;
let to_i16(what: bool) -> i16;
let to_u16(what: bool) -> u16;
let to_i32(what: bool) -> i32;
let to_u32(what: bool) -> u32;
let to_i64(what: bool) -> i64;
let to_u64(what: bool) -> u64;
let to_f32(what: bool) -> f32;
let to_f64(what: bool) -> f64;
