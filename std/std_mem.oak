/*
Jordan Dehmel, 2023
GPLv3

Standard mem function definitions for use
with mem.cpp.

Note: these three functions should only
be used within methods! Use outside of
methods should be considered dangerous,
as it will not fall out of scope with
its creator.
*/

// Allocate heap memory
// let alloc<t>(num: u128) -> ^t;
// let alloc<t>() -> ^t;

// Free heap memory
// let free<t>(to_free: ^t) -> void;
