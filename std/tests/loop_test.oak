// For testing Oak's loops
// Legacy passed

package!("std");
use_rule!("std");

let main() -> i32
{
    let flag_var = 10i32;

    while flag_var > 0i32
    {
        print(flag_var);
        print("\n");

        --flag_var;
    }

    0i32
}
