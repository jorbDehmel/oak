// For testing Oak's loops
// Legacy passed

package!("std");
use_rule!("std");

let main() -> i32
{
    let flag_var: i32;
    flag_var = 10;

    while (flag_var > 0)
    {
        print(flag_var);
        print("\n");

        flag_var -= 1;
    }

    0
}
