/*
Tests while loops in oak
*/

include!("std/std.oak");

let main() -> i32 {
  let counter: i32;
  counter = 0i32;

  while (counter < 128i32) {
    print(counter);
    endl();
    ++counter;
  }

  return 0i32;
}
