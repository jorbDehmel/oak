include!("std/std.oak");

let main() -> i32 {
  srand(123uint);

  let counter: i32;
  counter = 0i32;

  while (counter < 10i32) {
    let foo: int;
    foo = rand();

    print(counter);
    print("\t|\t");
    print(foo);
    endl();

    ++counter;
  }

  return 0i32;
}
