include!("std/std.oak");

let bundle: struct {
  a: i32,
  b: bool,
}

let New(self: ^bundle) -> void {
  self.a = 0i32;
  self.b = false;
}

let main() -> i32 {
  let foo: bundle;
  foo.a = 0i32;
  foo.b = true;

  print(foo.a);
  endl();

  print(foo.b);
  endl();

  return 0i32;
}
