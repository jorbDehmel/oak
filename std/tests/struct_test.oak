include!("std/std.oak");

let bundle: struct {
  a: i32,
  b: bool,
}

let main() -> i32 {
  let foo: bundle;
  Copy(foo.a, 0i32);
  Copy(foo.b, true);

  print(foo.a);
  endl();

  print(foo.b);
  endl();

  return 0i32;
}
