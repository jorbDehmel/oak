/*
Tests whether scopes and lines can be treated equivalently in
Oak

Jordan Dehmel, 2023
jdehmel@outlook.com
*/

package!("std");
use_rule!("std");

let main() -> i32
{
    if (true)
        if (true)
            print("Entered if statement.\n");

    print("This is not inside an if statement.\n");

    0i32
}
