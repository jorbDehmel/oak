/*
Tests implicit generation of `New` and `Del`
*/

include!("std/io.oak");

let ExampleStruct: struct {
  data: i32,
}

let Del(self: ^ExampleStruct) -> void {
  print("Hello from destructor\n");
}

let main() -> i32 {
  let instance: ExampleStruct;

  return 0i32;
}
