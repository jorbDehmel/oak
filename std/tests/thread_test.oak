// A test of Oak's multi-threading file "std/threads.oak"
// Legacy passed

package!("std");
use_rule!("std");
include!("std/thread.oak");

let call() -> void
{
    let counter = 100i32;
    while counter > 0i32
    {
        print(counter);
        print("\n");

        --counter;
    }
}

let mega_call() -> void
{
    let counter = 1000i32;
    while counter > 0i32
    {
        print(counter);
        print("\n");

        --counter;
    }
}

let main() -> i32
{
    let a: thread;
    let b: thread;
    let c: thread;
    let d: thread;
    let e: thread;

    a.start(@call);
    b.start(@mega_call);
    c.start(@call);
    d.start(@call);
    e.start(@call);

    a.join();
    b.join();
    c.join();
    d.join();
    e.join();

    0i32
}
