/*
Tests recursive function calls in Oak
*/

include!("std/std.oak");

let foo(i: i32) -> i32 {
  if (i <= 0i32) {
    return i;
  } else {
    return i + foo(i - 1i32);
  }
}

let main() -> i32 {
  print(foo(10i32));
  endl();
  return 0i32;
}
