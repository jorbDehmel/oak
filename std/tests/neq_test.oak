/*
Ensures that the lexer is not mistaking Neq operations for
pre proc macros.
*/

package!("std");
include!("std/panic.oak");
use_rule!("std");

let main() -> i32
{
    let item = 5i128;
    assert!(item != 9i128);
    0i32
}
