/*
Tests std/str.oak
*/

include!("std/std.oak", "std/string.oak");

let main() -> i32 {
  let s: String;
  s = "Hello, ";

  print(s);
  endl();

  s = s + "world!";
  print(s);
  endl();

  return 0i32;
}
