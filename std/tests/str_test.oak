/*
Tests std/str.oak
*/

include!("std/std.oak", "std/string.oak");

let main() -> i32 {
  let s, t, r: String;
  s = "Hello, ";
  t = "world!";

  print(s);
  endl();

  print(t);
  endl();

  r = s + t;
  print(r);
  endl();

  return 0i32;
}
