/**
 * @brief Tests namespace::use!("...")
 */

include!("std/io.oak");

let hamburger::cheeseburger::long::fn() -> void {
  print("Hello!\n");
}

namespace::use!(hamburger::cheeseburger::long);
namespace::use!(hamburger);

let main() -> i32 {
  fn();
  cheeseburger::long::fn();
  return 0i32;
}
