/*
Basic array testing
*/

package!("std");
use_rule!("std");

let main(c: i32, v: []str) -> i32
{
    // Iterate over the arguments passed to main
    let i = 0i32;
    while i < c
    {
        // Print the current argument
        println(v.Get(i));
        i += 1i32;
    }

    0i32
}
