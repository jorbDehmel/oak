// Linkage file. Use "std/io_inter.oak" instead whenever
// possible, as it will save the compiler a trip.

// Ensure that this will work with other dialects
tag!("no_dialect");

include!("std/io_inter.oak");
