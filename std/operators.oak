/**
 * @brief Standard operators for Oak: Oak binding. Autogen
 */

pragma!("no_dialect");
link!("std/operators.o");

let Add, Sub, Mult, Div, Mod(l: i8, r: i8) -> i8;
let Add, Sub, Mult, Div, Mod(l: u8, r: u8) -> u8;
let Add, Sub, Mult, Div, Mod(l: i16, r: i16) -> i16;
let Add, Sub, Mult, Div, Mod(l: u16, r: u16) -> u16;
let Add, Sub, Mult, Div, Mod(l: i32, r: i32) -> i32;
let Add, Sub, Mult, Div, Mod(l: u32, r: u32) -> u32;
let Add, Sub, Mult, Div, Mod(l: i64, r: i64) -> i64;
let Add, Sub, Mult, Div, Mod(l: u64, r: u64) -> u64;
let Add, Sub, Mult, Div, Mod(l: int, r: int) -> int;
let Add, Sub, Mult, Div, Mod(l: uint, r: uint) -> uint;
let Add, Sub, Mult, Div(l: f32, r: f32) -> f32;
let Add, Sub, Mult, Div(l: f64, r: f64) -> f64;
let Add, Sub, Mult, Div, Mod(l: bool, r: bool) -> bool;
let Eq, Neq, Less, Leq, Great, Greq(l: i8, r: i8) -> bool;
let Eq, Neq, Less, Leq, Great, Greq(l: u8, r: u8) -> bool;
let Eq, Neq, Less, Leq, Great, Greq(l: i16, r: i16) -> bool;
let Eq, Neq, Less, Leq, Great, Greq(l: u16, r: u16) -> bool;
let Eq, Neq, Less, Leq, Great, Greq(l: i32, r: i32) -> bool;
let Eq, Neq, Less, Leq, Great, Greq(l: u32, r: u32) -> bool;
let Eq, Neq, Less, Leq, Great, Greq(l: i64, r: i64) -> bool;
let Eq, Neq, Less, Leq, Great, Greq(l: u64, r: u64) -> bool;
let Eq, Neq, Less, Leq, Great, Greq(l: int, r: int) -> bool;
let Eq, Neq, Less, Leq, Great, Greq(l: uint, r: uint) -> bool;
let Eq, Neq, Less, Leq, Great, Greq(l: f32, r: f32) -> bool;
let Eq, Neq, Less, Leq, Great, Greq(l: f64, r: f64) -> bool;
let Eq, Neq(l: bool, r: bool) -> bool;
let Copy, AddEq, SubEq, MultEq, DivEq, ModEq
  (self: ^i8, what: i8) -> i8;
let Copy, AddEq, SubEq, MultEq, DivEq, ModEq
  (self: ^u8, what: u8) -> u8;
let Copy, AddEq, SubEq, MultEq, DivEq, ModEq
  (self: ^i16, what: i16) -> i16;
let Copy, AddEq, SubEq, MultEq, DivEq, ModEq
  (self: ^u16, what: u16) -> u16;
let Copy, AddEq, SubEq, MultEq, DivEq, ModEq
  (self: ^i32, what: i32) -> i32;
let Copy, AddEq, SubEq, MultEq, DivEq, ModEq
  (self: ^u32, what: u32) -> u32;
let Copy, AddEq, SubEq, MultEq, DivEq, ModEq
  (self: ^i64, what: i64) -> i64;
let Copy, AddEq, SubEq, MultEq, DivEq, ModEq
  (self: ^u64, what: u64) -> u64;
let Copy, AddEq, SubEq, MultEq, DivEq, ModEq
  (self: ^int, what: int) -> int;
let Copy, AddEq, SubEq, MultEq, DivEq, ModEq
  (self: ^uint, what: uint) -> uint;
let Copy, AddEq, SubEq, MultEq, DivEq
  (self: ^f32, what: f32) -> f32;
let Copy, AddEq, SubEq, MultEq, DivEq
  (self: ^f64, what: f64) -> f64;
let Copy(self: ^bool, what: bool) -> bool;
let Incr, Decr(self: ^i8) -> i8;
let Incr, Decr(self: ^u8) -> u8;
let Incr, Decr(self: ^i16) -> i16;
let Incr, Decr(self: ^u16) -> u16;
let Incr, Decr(self: ^i32) -> i32;
let Incr, Decr(self: ^u32) -> u32;
let Incr, Decr(self: ^i64) -> i64;
let Incr, Decr(self: ^u64) -> u64;
let Incr, Decr(self: ^int) -> int;
let Incr, Decr(self: ^uint) -> uint;
let Eq, Neq(l: []i8, r: []i8) -> bool;
let Orr, Andd(l: bool, r: bool) -> bool;
let AnddEq, OrrEq(self: ^bool, other: bool) -> bool;
let Not(x: bool) -> bool;
let Flip(x: ^bool) -> void;
