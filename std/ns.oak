/*
This is now a unit file. It used to provide the `namespace`
rule before this became built-in to the compilation process.

Jordan Dehmel, 2024
jdehmel@outlook.com
*/

// Ensure that this will work with other dialects
tag!("no_dialect");

// Raise deprecation warning.
c::warn!(
    "The `std/ns.oak` file is deprecated; Namespaces are "
    "built-in as of version 0.4.26."
);
