/*
Casts for Oak
*/

#include "oak/std/std_oak_header.h"

// to_u8(what: i8) -> u8
u8 to_u8_FN_i8_MAPS_u8(i8 what) {
  return what;
}

// to_i16(what: i8) -> i16
i16 to_i16_FN_i8_MAPS_i16(i8 what) {
  return what;
}

// to_u16(what: i8) -> u16
u16 to_u16_FN_i8_MAPS_u16(i8 what) {
  return what;
}

// to_i32(what: i8) -> i32
i32 to_i32_FN_i8_MAPS_i32(i8 what) {
  return what;
}

// to_u32(what: i8) -> u32
u32 to_u32_FN_i8_MAPS_u32(i8 what) {
  return what;
}

// to_i64(what: i8) -> i64
i64 to_i64_FN_i8_MAPS_i64(i8 what) {
  return what;
}

// to_u64(what: i8) -> u64
u64 to_u64_FN_i8_MAPS_u64(i8 what) {
  return what;
}

// to_f32(what: i8) -> f32
f32 to_f32_FN_i8_MAPS_f32(i8 what) {
  return what;
}

// to_f64(what: i8) -> f64
f64 to_f64_FN_i8_MAPS_f64(i8 what) {
  return what;
}

// to_bool(what: i8) -> bool
bool to_bool_FN_i8_MAPS_bool(i8 what) {
  return what;
}

// to_i8(what: u8) -> i8
i8 to_i8_FN_u8_MAPS_i8(u8 what) {
  return what;
}

// to_i16(what: u8) -> i16
i16 to_i16_FN_u8_MAPS_i16(u8 what) {
  return what;
}

// to_u16(what: u8) -> u16
u16 to_u16_FN_u8_MAPS_u16(u8 what) {
  return what;
}

// to_i32(what: u8) -> i32
i32 to_i32_FN_u8_MAPS_i32(u8 what) {
  return what;
}

// to_u32(what: u8) -> u32
u32 to_u32_FN_u8_MAPS_u32(u8 what) {
  return what;
}

// to_i64(what: u8) -> i64
i64 to_i64_FN_u8_MAPS_i64(u8 what) {
  return what;
}

// to_u64(what: u8) -> u64
u64 to_u64_FN_u8_MAPS_u64(u8 what) {
  return what;
}

// to_f32(what: u8) -> f32
f32 to_f32_FN_u8_MAPS_f32(u8 what) {
  return what;
}

// to_f64(what: u8) -> f64
f64 to_f64_FN_u8_MAPS_f64(u8 what) {
  return what;
}

// to_bool(what: u8) -> bool
bool to_bool_FN_u8_MAPS_bool(u8 what) {
  return what;
}

// to_i8(what: i16) -> i8
i8 to_i8_FN_i16_MAPS_i8(i16 what) {
  return what;
}

// to_u8(what: i16) -> u8
u8 to_u8_FN_i16_MAPS_u8(i16 what) {
  return what;
}

// to_u16(what: i16) -> u16
u16 to_u16_FN_i16_MAPS_u16(i16 what) {
  return what;
}

// to_i32(what: i16) -> i32
i32 to_i32_FN_i16_MAPS_i32(i16 what) {
  return what;
}

// to_u32(what: i16) -> u32
u32 to_u32_FN_i16_MAPS_u32(i16 what) {
  return what;
}

// to_i64(what: i16) -> i64
i64 to_i64_FN_i16_MAPS_i64(i16 what) {
  return what;
}

// to_u64(what: i16) -> u64
u64 to_u64_FN_i16_MAPS_u64(i16 what) {
  return what;
}

// to_f32(what: i16) -> f32
f32 to_f32_FN_i16_MAPS_f32(i16 what) {
  return what;
}

// to_f64(what: i16) -> f64
f64 to_f64_FN_i16_MAPS_f64(i16 what) {
  return what;
}

// to_bool(what: i16) -> bool
bool to_bool_FN_i16_MAPS_bool(i16 what) {
  return what;
}

// to_i8(what: u16) -> i8
i8 to_i8_FN_u16_MAPS_i8(u16 what) {
  return what;
}

// to_u8(what: u16) -> u8
u8 to_u8_FN_u16_MAPS_u8(u16 what) {
  return what;
}

// to_i16(what: u16) -> i16
i16 to_i16_FN_u16_MAPS_i16(u16 what) {
  return what;
}

// to_i32(what: u16) -> i32
i32 to_i32_FN_u16_MAPS_i32(u16 what) {
  return what;
}

// to_u32(what: u16) -> u32
u32 to_u32_FN_u16_MAPS_u32(u16 what) {
  return what;
}

// to_i64(what: u16) -> i64
i64 to_i64_FN_u16_MAPS_i64(u16 what) {
  return what;
}

// to_u64(what: u16) -> u64
u64 to_u64_FN_u16_MAPS_u64(u16 what) {
  return what;
}

// to_f32(what: u16) -> f32
f32 to_f32_FN_u16_MAPS_f32(u16 what) {
  return what;
}

// to_f64(what: u16) -> f64
f64 to_f64_FN_u16_MAPS_f64(u16 what) {
  return what;
}

// to_bool(what: u16) -> bool
bool to_bool_FN_u16_MAPS_bool(u16 what) {
  return what;
}

// to_i8(what: i32) -> i8
i8 to_i8_FN_i32_MAPS_i8(i32 what) {
  return what;
}

// to_u8(what: i32) -> u8
u8 to_u8_FN_i32_MAPS_u8(i32 what) {
  return what;
}

// to_i16(what: i32) -> i16
i16 to_i16_FN_i32_MAPS_i16(i32 what) {
  return what;
}

// to_u16(what: i32) -> u16
u16 to_u16_FN_i32_MAPS_u16(i32 what) {
  return what;
}

// to_u32(what: i32) -> u32
u32 to_u32_FN_i32_MAPS_u32(i32 what) {
  return what;
}

// to_i64(what: i32) -> i64
i64 to_i64_FN_i32_MAPS_i64(i32 what) {
  return what;
}

// to_u64(what: i32) -> u64
u64 to_u64_FN_i32_MAPS_u64(i32 what) {
  return what;
}

// to_f32(what: i32) -> f32
f32 to_f32_FN_i32_MAPS_f32(i32 what) {
  return what;
}

// to_f64(what: i32) -> f64
f64 to_f64_FN_i32_MAPS_f64(i32 what) {
  return what;
}

// to_bool(what: i32) -> bool
bool to_bool_FN_i32_MAPS_bool(i32 what) {
  return what;
}

// to_i8(what: u32) -> i8
i8 to_i8_FN_u32_MAPS_i8(u32 what) {
  return what;
}

// to_u8(what: u32) -> u8
u8 to_u8_FN_u32_MAPS_u8(u32 what) {
  return what;
}

// to_i16(what: u32) -> i16
i16 to_i16_FN_u32_MAPS_i16(u32 what) {
  return what;
}

// to_u16(what: u32) -> u16
u16 to_u16_FN_u32_MAPS_u16(u32 what) {
  return what;
}

// to_i32(what: u32) -> i32
i32 to_i32_FN_u32_MAPS_i32(u32 what) {
  return what;
}

// to_i64(what: u32) -> i64
i64 to_i64_FN_u32_MAPS_i64(u32 what) {
  return what;
}

// to_u64(what: u32) -> u64
u64 to_u64_FN_u32_MAPS_u64(u32 what) {
  return what;
}

// to_f32(what: u32) -> f32
f32 to_f32_FN_u32_MAPS_f32(u32 what) {
  return what;
}

// to_f64(what: u32) -> f64
f64 to_f64_FN_u32_MAPS_f64(u32 what) {
  return what;
}

// to_bool(what: u32) -> bool
bool to_bool_FN_u32_MAPS_bool(u32 what) {
  return what;
}

// to_i8(what: i64) -> i8
i8 to_i8_FN_i64_MAPS_i8(i64 what) {
  return what;
}

// to_u8(what: i64) -> u8
u8 to_u8_FN_i64_MAPS_u8(i64 what) {
  return what;
}

// to_i16(what: i64) -> i16
i16 to_i16_FN_i64_MAPS_i16(i64 what) {
  return what;
}

// to_u16(what: i64) -> u16
u16 to_u16_FN_i64_MAPS_u16(i64 what) {
  return what;
}

// to_i32(what: i64) -> i32
i32 to_i32_FN_i64_MAPS_i32(i64 what) {
  return what;
}

// to_u32(what: i64) -> u32
u32 to_u32_FN_i64_MAPS_u32(i64 what) {
  return what;
}

// to_u64(what: i64) -> u64
u64 to_u64_FN_i64_MAPS_u64(i64 what) {
  return what;
}

// to_f32(what: i64) -> f32
f32 to_f32_FN_i64_MAPS_f32(i64 what) {
  return what;
}

// to_f64(what: i64) -> f64
f64 to_f64_FN_i64_MAPS_f64(i64 what) {
  return what;
}

// to_bool(what: i64) -> bool
bool to_bool_FN_i64_MAPS_bool(i64 what) {
  return what;
}

// to_i8(what: u64) -> i8
i8 to_i8_FN_u64_MAPS_i8(u64 what) {
  return what;
}

// to_u8(what: u64) -> u8
u8 to_u8_FN_u64_MAPS_u8(u64 what) {
  return what;
}

// to_i16(what: u64) -> i16
i16 to_i16_FN_u64_MAPS_i16(u64 what) {
  return what;
}

// to_u16(what: u64) -> u16
u16 to_u16_FN_u64_MAPS_u16(u64 what) {
  return what;
}

// to_i32(what: u64) -> i32
i32 to_i32_FN_u64_MAPS_i32(u64 what) {
  return what;
}

// to_u32(what: u64) -> u32
u32 to_u32_FN_u64_MAPS_u32(u64 what) {
  return what;
}

// to_i64(what: u64) -> i64
i64 to_i64_FN_u64_MAPS_i64(u64 what) {
  return what;
}

// to_f32(what: u64) -> f32
f32 to_f32_FN_u64_MAPS_f32(u64 what) {
  return what;
}

// to_f64(what: u64) -> f64
f64 to_f64_FN_u64_MAPS_f64(u64 what) {
  return what;
}

// to_bool(what: u64) -> bool
bool to_bool_FN_u64_MAPS_bool(u64 what) {
  return what;
}

// to_i8(what: f32) -> i8
i8 to_i8_FN_f32_MAPS_i8(f32 what) {
  return what;
}

// to_u8(what: f32) -> u8
u8 to_u8_FN_f32_MAPS_u8(f32 what) {
  return what;
}

// to_i16(what: f32) -> i16
i16 to_i16_FN_f32_MAPS_i16(f32 what) {
  return what;
}

// to_u16(what: f32) -> u16
u16 to_u16_FN_f32_MAPS_u16(f32 what) {
  return what;
}

// to_i32(what: f32) -> i32
i32 to_i32_FN_f32_MAPS_i32(f32 what) {
  return what;
}

// to_u32(what: f32) -> u32
u32 to_u32_FN_f32_MAPS_u32(f32 what) {
  return what;
}

// to_i64(what: f32) -> i64
i64 to_i64_FN_f32_MAPS_i64(f32 what) {
  return what;
}

// to_u64(what: f32) -> u64
u64 to_u64_FN_f32_MAPS_u64(f32 what) {
  return what;
}

// to_f64(what: f32) -> f64
f64 to_f64_FN_f32_MAPS_f64(f32 what) {
  return what;
}

// to_bool(what: f32) -> bool
bool to_bool_FN_f32_MAPS_bool(f32 what) {
  return what;
}

// to_i8(what: f64) -> i8
i8 to_i8_FN_f64_MAPS_i8(f64 what) {
  return what;
}

// to_u8(what: f64) -> u8
u8 to_u8_FN_f64_MAPS_u8(f64 what) {
  return what;
}

// to_i16(what: f64) -> i16
i16 to_i16_FN_f64_MAPS_i16(f64 what) {
  return what;
}

// to_u16(what: f64) -> u16
u16 to_u16_FN_f64_MAPS_u16(f64 what) {
  return what;
}

// to_i32(what: f64) -> i32
i32 to_i32_FN_f64_MAPS_i32(f64 what) {
  return what;
}

// to_u32(what: f64) -> u32
u32 to_u32_FN_f64_MAPS_u32(f64 what) {
  return what;
}

// to_i64(what: f64) -> i64
i64 to_i64_FN_f64_MAPS_i64(f64 what) {
  return what;
}

// to_u64(what: f64) -> u64
u64 to_u64_FN_f64_MAPS_u64(f64 what) {
  return what;
}

// to_f32(what: f64) -> f32
f32 to_f32_FN_f64_MAPS_f32(f64 what) {
  return what;
}

// to_bool(what: f64) -> bool
bool to_bool_FN_f64_MAPS_bool(f64 what) {
  return what;
}

// to_i8(what: bool) -> i8
i8 to_i8_FN_bool_MAPS_i8(bool what) {
  return what;
}

// to_u8(what: bool) -> u8
u8 to_u8_FN_bool_MAPS_u8(bool what) {
  return what;
}

// to_i16(what: bool) -> i16
i16 to_i16_FN_bool_MAPS_i16(bool what) {
  return what;
}

// to_u16(what: bool) -> u16
u16 to_u16_FN_bool_MAPS_u16(bool what) {
  return what;
}

// to_i32(what: bool) -> i32
i32 to_i32_FN_bool_MAPS_i32(bool what) {
  return what;
}

// to_u32(what: bool) -> u32
u32 to_u32_FN_bool_MAPS_u32(bool what) {
  return what;
}

// to_i64(what: bool) -> i64
i64 to_i64_FN_bool_MAPS_i64(bool what) {
  return what;
}

// to_u64(what: bool) -> u64
u64 to_u64_FN_bool_MAPS_u64(bool what) {
  return what;
}

// to_f32(what: bool) -> f32
f32 to_f32_FN_bool_MAPS_f32(bool what) {
  return what;
}

// to_f64(what: bool) -> f64
f64 to_f64_FN_bool_MAPS_f64(bool what) {
  return what;
}
