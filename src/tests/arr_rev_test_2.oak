package!("std");
use_rule!("std");

let main(argc: i32, argv: []str) -> i32
{
    println<str>(_: str);

    let i: i32;
    i = 0;
    while (i < argc)
    {
        println(Get(argv, i));
        i += 1;
    }

    0
}
